# Діагностична прошивка ESP32 + Telegram бот

Прошивка для тестування ESP32-WROOM-32 перед підключенням до кавомашини Saeco Lirika OTC.

## Можливості

- **Telegram бот** (@Keksav_bot) з інлайн-кнопками — керування з будь-якої мережі
- **Веб-інтерфейс** (http://192.168.99.119) — керування з локальної мережі
- **Фізичні кнопки** — GPIO 12, 14, 27
- **4 LED індикатори** — GPIO 2, 4, 5, 18

## Схема підключення

### Світлодіоди (резистори 220Ω)
| GPIO | Колір | Підключення |
|------|-------|-------------|
| 2    | 🔴 Червоний | GPIO → 220Ω → LED(+) → LED(−) → GND |
| 4    | 🟢 Зелений  | GPIO → 220Ω → LED(+) → LED(−) → GND |
| 5    | 🔵 Синій    | GPIO → 220Ω → LED(+) → LED(−) → GND |
| 18   | 🟡 Жовтий   | GPIO → 220Ω → LED(+) → LED(−) → GND |

### Кнопки (INPUT_PULLUP)
| GPIO | Кнопка | Підключення |
|------|--------|-------------|
| 12   | 🟢 Зелена (ВКЛ/ВИМК) | GPIO ← кнопка → GND |
| 14   | 🔵 Синя (МЕНЮ)        | GPIO ← кнопка → GND |
| 27   | 🟡 Жовта (МІЦНІСТЬ)   | GPIO ← кнопка → GND |

## Telegram команди

Напишіть `/start` боту — з'явиться меню з кнопками:

- ⚡ **УВІМКНУТИ / ВИМКНУТИ** — живлення
- 🔴🟢🔵🟡 — перемикання окремих LED (● увімк / ○ вимк)
- 🔬 **Тест** — послідовне блимання всіх LED
- 🚫 **Все вимк** — вимкнути всі LED
- 📊 **Статус** — поточний стан

## Налаштування

Перед компіляцією змініть Wi-Fi та Telegram дані:

```cpp
const char* ssid = "YOUR_WIFI";
const char* password = "YOUR_PASSWORD";
const String BOT_TOKEN = "YOUR_BOT_TOKEN";
const String CHAT_ID = "YOUR_CHAT_ID";
```

## Залежності

- Arduino IDE 2.3.7+
- ESP32 board package 3.3.7+
- ArduinoJson 7.4.2+

## Завантаження

```bash
arduino-cli compile --fqbn esp32:esp32:esp32 .
arduino-cli upload --fqbn esp32:esp32:esp32 --port COM3 .
```
