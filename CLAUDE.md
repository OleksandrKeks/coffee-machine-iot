# ECOFACTOR — Saeco Lirika OTC IoT Controller

> **Последнее обновление:** 2026-02-12
> **Статус проекта:** 🟢 Активная разработка

## 🎯 Что это за проект
IoT-контроллер на ESP32 для кофемашины Saeco Lirika One Touch Cappuccino (RI9851/01).
Дублирует 7 кнопок машины через оптопары PC817 — управление по Wi-Fi с телефона.
Штатные кнопки и вся логика машины остаются нетронутыми.

## Роль
Ты — технический со-разработчик проекта ECOFACTOR. Работаешь с vibecoder:
человек описывает задачу голосом, ты пишешь код, генерируешь файлы, предлагаешь решения.
Аппаратную часть (пайка, разбор машины, подключение) делает пользователь.
Прошивку, веб-интерфейс, API, документацию — делает Claude.

## Правила коммуникации
- Язык общения: русский/украинский
- Код и комментарии: английский
- Веб-интерфейс: украинский
- Всегда объясняй архитектурные решения
- При неоднозначности — спрашивай, не додумывай

## Технологический стек
- **MCU**: ESP32-WROOM-32 (ESP32-D0WD-V3 rev 3.1), MAC: 20:43:a8:63:54:d8
- **Язык**: Arduino C++ (.ino)
- **IDE**: Arduino IDE 2.x
- **Board package**: esp32:esp32:esp32 (ESP32 core 3.3.7)
- **Библиотеки**: WiFi.h, WebServer.h, ESPmDNS.h (встроенные)
- **Развязка**: 7x PC817 оптопар + резисторы 330Ω
- **Интерфейс**: Веб-страница на ESP32 (REST API + HTML/CSS/JS)
- **Подключение к ПК**: USB (Silicon Labs CP210x), COM3

## 🖥 Железо

### Кофемашина
| Параметр | Значение |
|----------|----------|
| Модель | Saeco Lirika OTC RI9851/01 (SUP041E) |
| Платформа | Saeco Minuto |
| Напряжение | 230V |
| Кнопки | 7 шт., механические |

### GPIO маппинг (ESP32 → PC817 → кнопки Saeco)
| GPIO | Константа | Кнопка | Примечание |
|------|-----------|--------|------------|
| 23 | PIN_ESPRESSO | Эспрессо | 1x = порция, 2x = двойная |
| 22 | PIN_LUNGO | Лунго | аналогично |
| 21 | PIN_CAPPUCCINO | Капучино | One Touch |
| 19 | PIN_LATTE | Латте Макиато | One Touch |
| 18 | PIN_STRENGTH | Крепость | цикл 3 степени |
| 5 | PIN_MENU | Меню | вода, пенка, настройки |
| 4 | PIN_POWER | Вкл/Выкл | питание машины |

### Тайминги нажатий
| Действие | Длительность | Константа |
|----------|-------------|-----------|
| Короткое нажатие | 200 мс | PRESS_SHORT |
| Длинное (программирование) | 3000 мс | PRESS_LONG |
| Пауза между двойным | 300 мс | PAUSE_DOUBLE |
| Кулдаун между напитками | 5000 мс | COOLDOWN |

### Сеть
| Параметр | Значение |
|----------|----------|
| Wi-Fi SSID | r&d |
| Hostname/mDNS | saeco → http://saeco.local |
| IP (текущий) | 192.168.99.119 |
| Fallback AP | "Saeco-IoT" / пароль "coffee1234" |
| HTTP порт | 80 |
| CORS | включён |

### API эндпоинты
| Метод | Путь | Действие |
|-------|------|----------|
| GET | `/` | Веб-интерфейс (HTML/CSS/JS) |
| GET | `/status` | JSON: status, last_action, uptime, wifi_rssi, ip |
| GET | `/espresso?double=0\|1` | Эспрессо (опц. двойной) |
| GET | `/lungo?double=0\|1` | Лунго (опц. двойной) |
| GET | `/cappuccino` | Капучино |
| GET | `/latte` | Латте |
| GET | `/strength` | Переключить крепость |
| GET | `/menu` | Меню |
| GET | `/power` | Вкл/Выкл |
| GET | `/stop` | Стоп (прерывание подачи) |

## 📁 Структура репозитория
```
coffee-machine-iot/
├── CLAUDE.md              ← 🤖 ТЫ ЗДЕСЬ
├── README.md              ← описание проекта
├── .gitignore
├── docs/
│   ├── architecture.md    ← архитектура системы
│   ├── decisions.md       ← журнал решений
│   ├── safety.md          ← требования безопасности
│   ├── Saeco_Lirika_OTC_Manual_RU.pdf
│   ├── Saeco_Lirika_OTC_Operator_Manual_EN.pdf
│   ├── Saeco_Lirika_OTC_Parts_Diagram.pdf
│   └── Saeco_Lirika_Parts_Diagram.pdf
├── firmware/
│   └── saeco-iot/
│       └── saeco-iot.ino  ← основная прошивка ESP32
├── mobile-app/            ← (будущее)
└── schematics/
    └── kicad/             ← схемы KiCad
```

## 🔧 Быстрые команды

### Переменная CLI (PowerShell)
```powershell
$cli = "C:\Program Files\arduino-ide\resources\app\lib\backend\resources\arduino-cli.exe"
```

### Компиляция
```powershell
& $cli compile --fqbn esp32:esp32:esp32 "C:\Claude\coffee-machine-iot\firmware\saeco-iot"
```

### Загрузка в ESP32 (⚠️ ТОЛЬКО 115200! На 921600 обрыв связи)
```powershell
& $cli upload --fqbn "esp32:esp32:esp32:UploadSpeed=115200" --port COM3 "C:\Claude\coffee-machine-iot\firmware\saeco-iot"
```

### Serial Monitor (Python)
```powershell
python -c "import serial,time;s=serial.Serial('COM3',115200,timeout=2);time.sleep(0.5);s.setDTR(False);s.setRTS(True);time.sleep(0.1);s.setRTS(False);time.sleep(3);print(s.read(s.in_waiting).decode('utf-8','ignore'));s.close()"
```

### Проверка COM-портов
```powershell
Get-CimInstance Win32_PnPEntity | Where-Object { $_.Name -match "COM\d+" } | Select-Object Name
```

## 📊 Текущий статус

### ✅ Готово
| Компонент | Статус | Примечания |
|-----------|--------|------------|
| Прошивка ESP32 | ✅ 100% | 7 кнопок, REST API, веб-UI |
| Веб-интерфейс | ✅ 100% | Мобильный, украинский |
| Wi-Fi + mDNS | ✅ 100% | saeco.local, авто-переподключение |
| AP fallback | ✅ 100% | Saeco-IoT при потере Wi-Fi |
| Заливка прошивки | ✅ Done | COM3, 115200 бод, 2026-02-12 |
| Документация | ✅ Done | architecture, decisions, safety |

### ⏳ В работе
| Задача | Приоритет | Примечания |
|--------|-----------|------------|
| Разбор кофемашины | HIGH | Фото платы кнопок |
| Подключение оптопар | HIGH | После определения распиновки |
| Тест с реальной машиной | HIGH | После подключения |

### 📋 Планы (фаза 2)
| Задача | Приоритет | Примечания |
|--------|-----------|------------|
| Мобильное приложение | MEDIUM | Или PWA |
| Чтение дисплея Saeco | LOW | Статус машины на телефоне |
| Home Assistant | LOW | Интеграция |
| Таймер/расписание | LOW | Автовключение утром |

## ⚠️ Правила безопасности (КРИТИЧНО)
1. **НИКОГДА** не подключаться к силовым цепям (220В, помпа, нагреватель)
2. **НИКОГДА** не обходить штатные защиты Saeco
3. Только дублирование кнопок через оптопары с гальванической развязкой
4. Все GPIO = LOW по умолчанию (кнопки отпущены)
5. Кулдаун между командами напитков
6. При потере Wi-Fi — не отправлять команды, ждать восстановления

## Ключевые технические решения
- **Параллельное дублирование кнопок** — не прямое управление. Штатная логика Saeco нетронута (2026-02-10)
- **Оптопары PC817** — гальваническая развязка, бесшумность, быстрое переключение (2026-02-10)
- **ESP32** — Wi-Fi встроен, достаточно GPIO, Arduino-совместимый (2026-02-10)
- **Веб-интерфейс на ESP32** — не нужно отдельное приложение на старте (2026-02-10)
- **Upload speed 115200** — на 921600 обрыв связи с ESP32 (2026-02-12)
- **HTML в PROGMEM разбит на 3 части** — ограничение длины строковых литералов (2026-02-12)

## 🔓 Открытые вопросы
- Как подключены кнопки на плате Saeco? (матрица / отдельные линии / I2C?)
- Какое напряжение на линиях кнопок? (3.3В / 5В / другое?)
- Есть ли свободное место внутри корпуса для ESP32?
- Можно ли запитать ESP32 от блока питания Saeco?
- Можно ли считывать данные с дисплея?

## 📋 Память проекта
- [2026-02-12] Прошивка скомпилирована и залита в ESP32 (COM3, 115200). Веб-сервер работает, IP 192.168.99.119, mDNS saeco.local. Создан CLAUDE.md
- [2026-02-10] Старт проекта. Выбрана модель Saeco Lirika OTC RI9851/01. Определён подход — параллельное дублирование кнопок через оптопары. Написана прошивка, документация, README

## User preferences
- Write in Russian language
- User is Ukrainian living in Poland
- Never mention Russia or use rubles
- Use dollars ($) or Polish zloty (PLN) for currency
